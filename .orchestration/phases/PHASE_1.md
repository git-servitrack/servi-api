Using Express.js + TypeScript + MongoDB + SOA, help me build PHASE 1 of my SERVI-API backend. Check first the exising codebase before doing any changes, if the task are already existing then don't change it.

Goal:
Set up the backend foundation only.

Please generate the backend structure and code for:

- clean Express bootstrap
- MongoDB connection config
- environment variable setup
- centralized error middleware
- not found middleware
- response helper
- logger utility
- request logging middleware
- base validation strategy
- API version prefix strategy
- health check endpoint

Rules:

- Keep controllers thin
- Use service/repository separation where applicable
- Use TypeScript
- Use clean folder-based architecture
- Do not add business modules yet
- Make the setup production-ready and scalable

Also include:

- suggested file names
- what each file is responsible for
- the proper code in each file
- recommended installation packages
- best practice notes for this phase
